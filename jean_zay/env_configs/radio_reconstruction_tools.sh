#!/bin/bash
# Environment for radio_reconstruction_tools training/evaluation on Jean Zay.
# The package and its deps (radio_tools, deepinv, lightning, ptwt, torchkbnufft,
# finufft, wandb) are installed in this PyTorch module's Python.
module purge
module load pytorch-gpu/py3/2.7.0

